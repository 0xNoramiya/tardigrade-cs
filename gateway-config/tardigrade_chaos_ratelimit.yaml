# Chaos VM #2 — "rate-limit storm".
# Priority-0 target is the broken AWS Bedrock integration with the retry config
# tuned for 429-class status codes (3 attempts, 80ms backoff). Models the
# user-visible experience of "primary throttled, fall over fast".
name: tardigrade-chaos-ratelimit
type: provider-account/virtual-model
collaborators:
  - role_id: provider-account-manager
    subject: user:xnoramiya@gmail.com
  - role_id: provider-account-access
    subject: team:everyone
integrations:
  - name: tardigrade-chaos-ratelimit
    type: integration/model/virtual
    model_types: [chat]
    routing_config:
      type: priority-based-routing
      load_balance_targets:
        - target: aws-bedrock/global.anthropic.claude-opus-4-8
          priority: 0
          fallback_candidate: true
          fallback_status_codes: ["400", "401", "403", "404", "408", "429", "500", "502", "503", "504"]
          retry_config:
            attempts: 3
            delay: 80
            on_status_codes: ["429", "500", "502", "503", "504"]
        - target: openai/gpt-4o-mini
          priority: 1
          fallback_candidate: true
          fallback_status_codes: ["401", "403", "404", "408", "429", "500", "502", "503", "504"]
          retry_config:
            attempts: 1
            delay: 100
            on_status_codes: ["429", "500", "502", "503", "504"]
